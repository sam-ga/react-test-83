import styles from './HootUpdate.module.scss'

import HootForm from '../../components/HootForm/HootForm'

const HootUpdate = () => {
  return (
    <main className={styles.container}>
      <HootForm />
    </main>
  )
}

export default HootUpdate
