import styles from './HootCreate.module.scss'

import HootForm from '../../components/HootForm/HootForm'

const HootCreate = () => {
  return (
    <main className={styles.container}>
      <HootForm />
    </main>
  )
}

export default HootCreate
